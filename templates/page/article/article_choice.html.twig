{% extends 'base.html.twig' %}

{% block body %}
    <main>
        <section>
            <article class="bloc_articleSolo">
                <h1>Article choisis :</h1>
                <h2>{{ article.title }}</h2>
                <div class="img_article">
                    <img src="{{ article.image }}" alt="{{ article.title }}">
                </div>
                <p>{{ article.description }}</p>}
                <p>{{ article.content }}</p>}
                <h3>Date de creation : {{ article.createdAt | date("m-d-Y") }}</h3>
                <h3>Date de mise à jour : {{ article.updatedAt | date("m-d-Y") }}</h3>
            </article>
        </section>

    </main>
{% endblock %}